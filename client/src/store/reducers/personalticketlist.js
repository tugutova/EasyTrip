// import * as types from '../types/personalarea';

// export default function getPersonalTicketListReducer(state = {}, action) {
//   const { type, payload } = action;

//   switch (type) {

//     case types.GET_USER_PERSONAL_TICKET_LIST: {
//       const newState = { ...state };
//     }
//   }

// }
